apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: models
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  tls:
    - hosts: [models.andrea-house.com]
      secretName: models-andrea-house-tls
  rules:
    - host: models.andrea-house.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port: { name: http }
