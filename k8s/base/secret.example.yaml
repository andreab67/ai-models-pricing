# Do NOT commit real values. Replace with SealedSecret / ExternalSecret in production.
apiVersion: v1
kind: Secret
metadata:
  name: model-pricing-secrets
type: Opaque
stringData:
  DATABASE_URL: postgresql+asyncpg://pricing:CHANGE_ME@postgres:5432/pricing
  POSTGRES_PASSWORD: CHANGE_ME
  SMTP_USERNAME: pricing-bot
  SMTP_PASSWORD: CHANGE_ME
