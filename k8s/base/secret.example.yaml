# Do NOT commit real values. Replace with SealedSecret / ExternalSecret in production.
# SMTP credentials live in the separate ses-creds secret (same as ntp-checker).
apiVersion: v1
kind: Secret
metadata:
  name: model-pricing-secrets
type: Opaque
stringData:
  DATABASE_URL: postgresql+psycopg://model_pricing:CHANGE_ME@pgvector-cluster.pgvector.svc.cluster.local:5432/aimodels
  REDIS_URL: redis://:Refer-Mind0-Sleep-Square-Who@redis.redis.svc.cluster.local:6379/0
  OPENAI_API_KEY: sk-...
  ANTHROPIC_API_KEY: sk-ant-...
